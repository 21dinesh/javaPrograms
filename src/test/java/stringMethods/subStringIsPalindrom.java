package stringMethods;

public class subStringIsPalindrom {
	
	public static String palindrom(String subStr) {
		for (int  i=0;i<subStr.length();i++) {
			char Fch=subStr.charAt(i);
			char Lch=subStr.charAt(subStr.length())
		}
		return subStr;
	}
	public static void main(String[] args) {
			String str="INDIAI";
			for (int i=0;i<str.length();i++)
			{    String subStr="";
				for (int j=i;j<str.length();j++) {
					subStr= subStr+ str.charAt(j);
					if ((subStr)) {
					System.out.println(subStr);
					}
				}
			}
		}
		

	}



